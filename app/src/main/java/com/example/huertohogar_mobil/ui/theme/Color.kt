package com.example.huertohogar_mobil.ui.theme

import androidx.compose.ui.graphics.Color

// Paleta Natural - HuertoHogar

// Verdes (Primary)
val GreenPrimary = Color(0xFF2E7D32) // Verde bosque profundo
val GreenPrimaryContainer = Color(0xFFA5D6A7) // Verde suave para fondos de botones/cards
val GreenOnPrimary = Color(0xFFFFFFFF)

// Tierras / Naranjas (Secondary)
val EarthSecondary = Color(0xFFD84315) // Terracota
val EarthSecondaryContainer = Color(0xFFFFCCBC) // Durazno suave
val EarthOnSecondary = Color(0xFFFFFFFF)

// Acentos (Tertiary)
val SunTertiary = Color(0xFFF9A825) // Amarillo mostaza/sol
val SunTertiaryContainer = Color(0xFFFFF59D)

// Neutrales y Fondos
val CreamBackground = Color(0xFFFCFBF2) // Crema muy suave (no blanco puro, cansa menos la vista)
val SurfaceWhite = Color(0xFFFFFFFF)
val SurfaceVariant = Color(0xFFF0F4C3) // Verde lima muy pálido para tarjetas

// Textos
val TextBlack = Color(0xFF1B1B1B) // Negro suave
val TextGrey = Color(0xFF555555)

// Modo Oscuro (Adaptado para no encandilar)
val GreenPrimaryDark = Color(0xFF81C784)
val EarthSecondaryDark = Color(0xFFFFAB91)
val BackgroundDark = Color(0xFF121212)
val SurfaceDark = Color(0xFF1E1E1E)
